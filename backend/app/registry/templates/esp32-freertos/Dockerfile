# ESP-IDF ya incluye FreeRTOS internamente (es la base de todo firmware
# ESP-IDF, incluso el "bare metal" técnicamente corre sobre FreeRTOS).
# Se mantiene como estaba.
FROM espressif/idf:latest

WORKDIR /workspace/src